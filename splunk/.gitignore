# Packer cache and crash logs
packer_cache/
crash.log

# Variable files - every value here comes from the environment at call time, so
# a pkrvars file in this directory would only ever be a stray secret.
*.pkrvars.hcl
*.auto.pkrvars.hcl
